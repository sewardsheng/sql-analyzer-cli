# 规则目录结构说明

## 📁 目录结构

```
rules/
├── approved/                    # 正式发布的规则（生产环境使用）
│   └── [category]/[rule].md
└── learning-rules/              # 学习型规则（整个学习周期）
    ├── generated/               # 🆕 AI生成的原始规则（待评估）
    │   └── [rule].md
    ├── approved/                # ✅ 评估优秀规则（建议批准）
    │   └── [category]/[rule].md
    ├── manual_review/           # 👥 需要人工审核的规则
    │   └── [category]/[rule].md
    ├── low_quality/             # ❌ 质量低下的规则
    │   └── [category]/[rule].md
    ├── duplicates/              # 🔄 重复的规则
    │   └── [category]/[rule].md
    └── issues/                  # 🐛 有问题的规则（格式错误等）
        └── [category]/[rule].md
```

## 🔄 处理流程

### 1. 规则生成阶段
```
AI生成 → rules/learning-rules/generated/ (新生规则，待评估)
```

### 2. 规则评估阶段
```
批量评估（从 generated 目录读取）
  ↓
根据评估结果自动分类：
  - 优秀规则 (≥90分) → rules/learning-rules/approved/
  - 良好规则 (70-89分) → rules/learning-rules/manual_review/
  - 低质量规则 (<70分) → rules/learning-rules/low_quality/
  - 重复规则 → rules/learning-rules/duplicates/
  - 格式错误 → rules/learning-rules/issues/
```

### 3. 人工审批阶段
```
人工审核 generated 中的待审核规则
  ↓
评估引擎自动分类：
  - 优秀规则 → manual_review (需要人工最终确认)
  - 良好规则 → manual_review (需要人工审核)
  - 低质量规则 → low_quality/
  - 重复规则 → duplicates/
  - 格式问题 → issues/
  ↓
人工最终审批 manual_review 中的规则：
  - 批准 → rules/approved/ (正式发布)
  - 拒绝 → rules/learning-rules/issues/ (废弃)
```

## 🔧 各组件配置

### CLI 命令
- **评估命令**: 默认从 `rules/learning-rules/generated` 读取
- **学习命令**: 生成新规则保存到 `rules/learning-rules/generated`
- **审批命令**: 从 `rules/learning-rules/generated` 读取待审核规则，评估后从 `manual_review` 读取最终确认

### API 接口
- **分析接口**: 规则学习保存到 `rules/learning-rules/generated`
- **评估接口**: 支持自定义源目录，推荐使用 `generated`

### 评估引擎
- **目标目录**: 根据分类结果自动移动到对应子目录
- **目录创建**: 自动创建所有必要的目录结构

## 💡 业务逻辑说明

1. **generated**: 存放AI生成但未评估的原始规则
2. **manual_review**: 存放评估后需要人工进一步审核的规则
3. **approved**: 存放评估质量良好，建议批准的规则
4. **low_quality**: 存放质量不高但可能有价值的规则
5. **duplicates**: 存放检测到的重复规则
6. **issues**: 存放格式错误或其他问题的规则

## 🚀 使用示例

```bash
# 1. 从历史记录学习生成规则
npm run learn

# 2. 评估生成的规则（从 generated 读取）
npm run evaluate -- --source rules/learning-rules/generated --move

# 3. 审批待审核规则（从 generated 读取，自动评估+人工最终确认）
npm run menu -- --action approval
```

## ✅ 修改历史

- **2025-12-02**: 修正业务逻辑，新规则生成保存到 `generated` 而非 `manual_review`
- **2025-12-02**: 修正审批逻辑，从 `generated` 读取待审核规则而非 `manual_review`
- **修改文件**: 规则生成器、学习器、CLI命令、API路由、评估配置、Menu审批功能